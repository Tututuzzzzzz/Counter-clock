import 'package:flutter/material.dart';

/// Color constants for the app
class ColorConstants {
  // Primary colors
  static const Color primaryColor = Colors.deepPurple;
  static const Color accentColor = Colors.deepPurpleAccent;
  
  // Timer status colors
  static const Color safeColor = Colors.green;
  static const Color warningColor = Colors.orange;
  static const Color criticalColor = Colors.red;
  
  // Button colors
  static const Color startButtonColor = Colors.green;
  static const Color pauseButtonColor = Colors.orange;
  static const Color resetButtonColor = Colors.blue;
  static const Color buttonTextColor = Colors.white;
  
  // UI colors
  static const Color errorColor = Colors.red;
  static const Color errorIconColor = Colors.red;
  static const Color infoCardBackground = Color(0xFFE3F2FD);
  
  ColorConstants._();
}
